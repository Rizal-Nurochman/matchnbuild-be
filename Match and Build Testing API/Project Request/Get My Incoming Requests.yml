info:  name: Get My Incoming Requests
  type: http
  seq: 4
http:
  method: GET
  url: "{{base_url}}/project-request/incoming"
  headers:    - name: Authorization
      value: "Bearer {{access_token}}"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5