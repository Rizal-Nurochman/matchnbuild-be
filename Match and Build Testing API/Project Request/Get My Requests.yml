info:
  name: Get My Requests
  type: http
  seq: 3
http:
  method: GET
  url: "{{base_url}}/project-request/my-requests"
  headers:
    - name: Authorization
      value: "Bearer {{access_token}}"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5