info:
  name: Send Password Reset
  type: http
  seq: 5

http:
  method: POST
  url: "{{bas_url}}/auth/send-password-reset"
  body:
    type: json
    data: |-
      {
        "email":"dejongfrenkie26@gmail.com"
      }
  auth: inherit

runtime:
  variables:
    - name: bas_url
      value: http://localhost:8888/api/v1

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
