info:
  name: Get quotations by ID project_request
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/quotation/project-request/{{project_request_id}}"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
